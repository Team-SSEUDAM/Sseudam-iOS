//
//  {{ feature }}XCTest.swift
//
//  {{ feature }}
//
//  Created by {{ author }}
//

import Testing
import ComposableArchitecture

@testable import {{ feature }}

@MainActor
struct {{ feature }}FeatureTest {
  @Test
  func addTest() async {
    let store = TestStore(
      initialState: {{ feature }}Feature.State(),
      reducer: { {{ feature }}Feature() }
    )
    
    await store.send(\.addButtonTapped) {
      $0.count += 1
    }
  }
  
}

